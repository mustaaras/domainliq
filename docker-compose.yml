services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - AUTH_SECRET=${AUTH_SECRET}
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:3000/api/health" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      - "traefik.docker.network=coolify"
      # Manual Wildcard Configuration (The Nuclear Fix)
      # HTTP Router (Redirects to HTTPS)
      - "traefik.http.routers.domainliq-wildcard-http.rule=Host(`test.domainliq.com`)"
      - "traefik.http.routers.domainliq-wildcard-http.entrypoints=http"
      - "traefik.http.routers.domainliq-wildcard-http.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      # HTTPS Router
      - "traefik.http.routers.domainliq-wildcard.rule=Host(`test.domainliq.com`)"
      - "traefik.http.routers.domainliq-wildcard.entrypoints=https"
      - "traefik.http.routers.domainliq-wildcard.tls=true"
      - "traefik.http.routers.domainliq-wildcard.tls.certresolver=letsencrypt"
      # FORCE Wildcard Certificate
      - "traefik.http.routers.domainliq-wildcard.tls.domains[0].main=domainliq.com"
      - "traefik.http.routers.domainliq-wildcard.tls.domains[0].sans=*.domainliq.com"
      # Unique Service Name to avoid conflicts
      - "traefik.http.routers.domainliq-wildcard.service=domainliq-manual-svc"
      - "traefik.http.services.domainliq-manual-svc.loadbalancer.server.port=3000"
    networks:
      - coolify

networks:
  coolify:
    external: true
